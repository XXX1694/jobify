# Base path the frontend calls. In dev this is proxied to the Go API by Vite.
VITE_API_BASE_URL=/api/v1

# Where the Vite dev proxy forwards /api requests (the running Go backend).
VITE_API_PROXY=http://localhost:8080
